@model DashboardViewModel

<div class="grid-row dashboard-header">
    <div class="column-two-thirds">
        <h1 class="heading-large small-bottom-margin">Recruit an apprentice</h1>
    </div>
</div>

<div asp-condition="Model.HasWarning" class="warning-summary" role="alert" aria-labelledby="error-summary-heading-example-1" data-valmsg-summary="true" tabindex="-1">
    <p>@Model.WarningMessage</p>
</div>

<div class="grid-row">
    <div class="column-full">
        <a asp-route="@RouteNames.CreateVacancy_Get" class="button">Create vacancy</a>
        <hr>
    </div>
</div>

<div class="grid-row">
    <div class="column-full">
        <table>
            <thead>
                <tr>
                    <th class="bold">Vacancy title</th>
                    <th class="bold">Status</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var vacancy in Model.Vacancies)
                {
                <tr>
                    <td><a asp-route="@RouteNames.DisplayVacancy_Get" asp-route-vacancyId="@vacancy.Id">@vacancy.Title</a></td>
                    <td>@vacancy.Status</td>
                </tr>
                }
            </tbody>
        </table>
    </div>
</div>