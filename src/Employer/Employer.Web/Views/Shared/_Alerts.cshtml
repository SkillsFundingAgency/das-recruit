@model Esfa.Recruit.Employer.Web.ViewModels.Alerts.AlertsViewModel

@if (Model.ShowWithdrawnByQaVacanciesAlert)
{
    <partial name="@PartialNames.WithdrawnByQaVacanciesAlert" for="WithdrawnByQaVacanciesAlert" />
}
@if (Model.ShowBlockedProviderAlert)
{
    <partial name="@PartialNames.BlockedProviderAlert" for="BlockedProviderAlert" />
}
@if (Model.ShowEmployerRevokedTransferredVacanciesAlert)
{
    <partial name="@PartialNames.EmployerRevokedTransferredVacanciesAlert" for="EmployerRevokedTransferredVacanciesAlert" />
}
@if (Model.ShowBlockedProviderTransferredVacanciesAlert)
{
    <partial name="@PartialNames.BlockedProviderTransferredVacanciesAlert" for="BlockedProviderTransferredVacanciesAlert" />
}
@if (DateTime.UtcNow < new DateTime(2025, 02, 17))
{
    <partial name="_NationalApprenticeshipWeekBanner"/>
}